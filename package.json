{
	"name": "google-maps-test",
	"version": "1.0.0",
	"description": "",
	"main": "Gruntfile.js",
	"scripts": {
		"start": "grunt serve",
		"prepare-win": "npm install -g bower grunt",
		"prepare-mac": "sudo npm install -g bower grunt",
		"serve-win": "npm install & bower install & grunt serve",
		"serve-mac": "npm install && bower install && grunt serve"
	},
	"devDependencies": {
		"grunt": "^1.0.1",
		"grunt-concurrent": "^2.3.1",
		"grunt-contrib-cssmin": "^2.0.0",
		"grunt-contrib-watch": "^1.0.0",
		"grunt-sass": "^1.2.1",
		"grunt-shell": "^2.0.0",
		"load-grunt-tasks": "^3.5.2",
		"serve": "^1.4.0"
	},
	"author": "Pablo Bassil",
	"license": "ISC"
}
